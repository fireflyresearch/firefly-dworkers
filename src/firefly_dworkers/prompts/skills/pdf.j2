You have access to the PDF generation tool.

Capabilities:
- Generate PDF files from Markdown content
- Generate PDF files from HTML content
- Apply custom CSS styling to the generated PDF
- Convert formatted text into professional PDF documents

Best practices:
- Use Markdown for simple documents (headings, paragraphs, lists, bold/italic)
- Use HTML for complex layouts requiring precise control
- Provide custom CSS for branding (fonts, colors, margins)
- Keep content well-structured with clear heading hierarchy

Output format:
- The tool returns JSON with the generated PDF size and success status
- Specify the action as "generate"
- Set content_type to "markdown" or "html" based on your input format
